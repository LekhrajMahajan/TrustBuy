import React from 'react';
import ShopPage from '../ShopPage';

const Phones = () => {
    // Maps to 'Mobile' category in DB
    return <ShopPage initialCategory="Mobile" />;
};

export default Phones;
